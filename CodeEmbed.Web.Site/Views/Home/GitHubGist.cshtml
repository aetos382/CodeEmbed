@{
  ViewBag.Title = "GtHub-Gist";

  var apiEndpoint = Configuration.ApiEndpoint;

  string sourceUri = new Uri(apiEndpoint, "github-gist/d80103a9d1c3c12b1786/HelloWorld.cs").AbsoluteUri;
}

@section head
{
  <link rel="stylesheet" type="text/css" href="@Url.Content("~/Content/lightview/lightview.css")"/>
}

<h2>@ViewBag.Title</h2>

<h3>必要なもの</h3>
<ul>
  <li>Gist の ID</li>
  <li>ファイル名</li>
</ul>

<p>
  <a href="@Url.Content("~/Content/Images/gist.png")" class="lightview" data-lightview-options="skin: 'dark'">
    <img src="@Url.Content("~/Content/Images/gist.png")" class="img-thumbnail" width="400" height="260" title="クリックで拡大"/>
  </a>
</p>

<h3>URL</h3>
<p>@(apiEndpoint)github-gist/&lt;gist の ID&gt;/&lt;ファイル名&gt;</p>
<p>例：@(apiEndpoint)github-gist/d80103a9d1c3c12b1786/HelloWorld.cs</p>

<h3>デモ</h3>

<h4>コード</h4>
<div>
<pre class="brush: js">
  $.ajax("@(apiEndpoint)github-gist/d80103a9d1c3c12b1786/HelloWorld.cs", {
    success: function (data) {
      $('#code').text(data);
      SyntaxHighlighter.highlight();
    }
  });
</pre>
</div>

<h4>結果</h4>
<div>
  <pre id="code" class="brush: csharp"></pre>
</div>

@section scripts
{
  <script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/swfobject/2.2/swfobject.js"></script>
  <script type="text/javascript" src="@Url.Content("~/bundles/LightView")"></script>

  <script type="text/javascript">

  $(function () {

    $.ajax("@sourceUri", {

      success: function (data) {

        $('#code').text(data);

        SyntaxHighlighter.defaults['auto-links'] = false;
        SyntaxHighlighter.highlight();

      }
    });

  });

  </script>
}
